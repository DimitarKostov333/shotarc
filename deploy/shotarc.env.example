# Copy to deploy/shotarc.env (gitignored) and fill in. Shared by the app build and the server.
# Generate values with:  head -c 24 /dev/urandom | base64 | tr -d '/+=' | head -c 32
INGEST_KEY=
DASHBOARD_KEY=

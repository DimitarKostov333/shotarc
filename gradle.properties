org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Where the app uploads sessions and where the APK is served from.
golfServerUrl=https://shotarc.co.za
# The ingest secret is NOT kept here — it would be committed. It is read from deploy/shotarc.env
# (gitignored) at build time. Copy deploy/shotarc.env.example to deploy/shotarc.env and fill it in.
